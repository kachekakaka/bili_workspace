# DOC-GOVERNANCE-20260803：测试入口与授权边界收口方案

- 待办 ID：`DOC-GOVERNANCE-20260803`
- 状态：已被替代
- 治理路线：标准骨架日常检查后的结构收口
- 来源：2026-08-03 `project-doc-skeleton` 只读骨架检查
- 用户决定：原施工目标由两个独立待办承接；本方案已按“被替代”退出

## 1. 本方案当前职责

1. 保存本轮已经确认的结构判断、职责拆分和交接边界，不再把测试入口实现与用户更新入口退役并入同一施工方案。
2. 为两个新待办建立唯一活动入口；两个待办均保持 `待确认`，在分别完成独立检查和保存前不创建活动方案。
3. 保留当前标准文档骨架、Registry schema、SAFETY、归档路径和停止正式发布决策，不进行结构迁移，不裁决测试结果、测试实现、产品事实、数据架构或具体运维流程。

## 2. 当前结构证据

### 2.1 T-PROJECT suite 契约不能由当前骨架证据闭合

- [`SoftwareTesting/project/README.md`](../../../SoftwareTesting/project/README.md) 已声明 Registry ID、执行类别、工作目录、输入、唯一职责、现有规范入口和失败语义。
- [`SoftwareTesting/SAFETY.md`](../../../SoftwareTesting/SAFETY.md) 保存隔离数据、进程、fixture、缓存、日志、输出和清理的公共规则。
- 当前直接证据不足以在不判断测试实现的前提下补齐 T-PROJECT 的独有触发、fixture、依赖、输出、断言和专项清理；把现有规则改写成完整契约会超出骨架职责。

结构结论：保留现有 suite 与 Registry，不编造正文、不赋予动态测试结果，也不改变 Registry schema 或 SAFETY；测试入口及其证据由独立实现待办承接，完成后再发起相应文档更新。

### 2.2 update.bat 属于待退出的活动入口

- `update.bat` 是当时的根目录 Windows 用户入口，并串联 Git 更新、验证和产品启动。
- [`scripts/README.md`](../../../scripts/README.md) 与 [`docs/运维/发布与回滚流程.md`](../../../docs/运维/发布与回滚流程.md) 承接其当时说明；现有结构测试和源码边界断言也直接消费该入口。
- 用户已经决定让 `update.bat` 退出活动入口，但实际删除、说明同步和直接消费者调整不在本方案施工范围内。

结构结论：由独立退役待办重新冻结直接消费者、可恢复方式和授权边界；本方案只保存退出决定和交接目标。

### 2.3 当前方案的生命周期

- 本方案在活动阶段曾由 [`docs/已知问题与待做需求.md`](../../../docs/已知问题与待做需求.md) 实际链接，并在保存阶段保持 `待确认`。
- 两个新待办建立后，本方案原施工目标已经完成职责拆分，但尚未取得生命周期关闭授权。
- 用户已经选择在关闭阶段把本方案记为“被替代”，不得误写为“已完成”。

## 3. 已确认的职责拆分

### 3.1 TEST-ENTRY-20260803

- 标题：T-PROJECT 隔离规范入口与契约证据。
- 初始状态：`待确认`，没有活动方案。
- 目标：为 T-PROJECT 建立有直接证据支撑、符合现有 SAFETY 的隔离规范入口，并产出可供 suite 契约引用的证据。
- 边界：不预定脚本、命令或实现方式；不修改 Registry schema；不放宽 SAFETY；不预先写入动态测试结果；实现完成后再由独立文档工作更新 suite 契约。
- ID 证据：当前活动文档和归档中未发现同名 ID；Git 历史因未获 Git 授权而未核对，施工前仍需补证，发现重复时停止并重新裁决 ID。

### 3.2 UPDATE-ENTRY-20260803

- 标题：退役 `update.bat` 及入口接线。
- 初始状态：`待确认`，没有活动方案。
- 目标：使 `update.bat` 退出当前用户入口，并同步更新全部直接文档入口和结构机械断言。
- 边界：不在该待办中重新设计数据目录、测试入口或发布流程；施工前重新冻结全部直接引用和消费者；文件修改、删除、命令、Git 和验证分别授权。
- ID 证据：当前活动文档和归档中未发现同名 ID；Git 历史因未获 Git 授权而未核对，施工前仍需补证，发现重复时停止并重新裁决 ID。

### 3.3 DOC-GOVERNANCE-20260803 的退出决定

- 两个新待办先建立活动入口，本方案再退出，不能先移除当前承接。
- 关闭时把本方案移动到 `archive/docs/plans/`，在计划索引新增“已被替代计划”小节，并由 `archive/docs/README.md` 恰好登记一次。
- 归档登记的当前承接指向活动待办入口，由其中两个新待办继续承接后续工作。
- 实际关闭、移动、索引更新和空条件目录处理需要用户另行验收与授权，本次保存不执行。

## 4. 目标路径与动作边界

### 4.1 本次保存阶段

- `docs/已知问题与待做需求.md`
- `docs/方案/DOC-GOVERNANCE-20260803-测试入口与授权边界收口.md`

本次只保存两个新待办、职责拆分和当前方案的退出决定；不创建两个新待办的活动方案。

### 4.2 后续关闭阶段候选路径

- `docs/已知问题与待做需求.md`：移除当前待办与活动方案链接，保留两个新待办。
- `docs/方案/DOC-GOVERNANCE-20260803-测试入口与授权边界收口.md`：内容完整移动到 `archive/docs/plans/`，归档状态记为“已被替代”。
- `archive/docs/plans/README.md`：新增“已被替代计划”小节并登记本方案。
- `archive/docs/README.md`：登记本方案的历史职责和当前承接。
- `docs/方案/`：只有移动后确认目录为空，且另行取得精确删除授权时，才清理该空条件目录。

上述关闭动作均未获本次保存授权。

### 4.3 当前方案不承接的路径

- T-PROJECT 的脚本、测试、运行器、suite 正文和安全实现由 `TEST-ENTRY-20260803` 独立检查后确定，本方案不预列施工文件。
- `update.bat`、说明文档和直接结构消费者由 `UPDATE-ENTRY-20260803` 独立检查后确定，本方案不实施退役。
- 本方案不授权修改产品源码、产品行为断言、`CONTEXT.md`、ADR 正文、活动发布 workflow、Registry schema、SAFETY、归档正文或真实运行数据；不包含施工、关闭、命令、Git、安装、测试、发布、删除或产品进程授权。

## 5. 完成条件

- `TEST-ENTRY-20260803` 和 `UPDATE-ENTRY-20260803` 以 `待确认`、无活动方案的状态登记在当前待办入口。
- 当前方案准确记录两个交接项、各自边界和用户确认的“被替代”退出决定，不再包含跨职责施工范围。
- 保存阶段不修改 T-PROJECT suite、SAFETY、Registry、`update.bat`、运维正文、测试代码或产品行为。
- 用户另行验收并授权关闭后，当前待办和活动方案退出，归档方案与两个索引形成一致导航；未获关闭授权时继续保持活动状态。
- 候选 ID 的 Git 历史唯一性在相关施工前补证；若发现重复，不沿用当前 ID。

## 6. 验证要求

- 测试层级：普通验证。
- 验证影响域：两个新待办的 ID、状态与活动方案数量，当前方案链接和测试决策字段，以及后续被替代方案的归档登记、当前承接和条件目录生命周期。
- 具体验证项：本次保存后静态复核两个新待办均为 `待确认` 且没有活动方案、当前方案仍由原待办实际链接、方案中三项测试决策各出现一次；获得独立命令授权后运行 T-DOC 规则夹具和目标项目门禁并比较标准资产；获得关闭授权后对最终生命周期状态再次运行适用 T-DOC。所有动态验证在实际执行前保持 `not_run`。

任何验证命令执行前都必须建立仅限本次任务的临时执行账本；本次保存未获得命令、Git、测试或安装授权。

## 7. 用户决定

- 当前状态：待确认。
- 用户已经确认职责拆分、两个候选 ID、两个新待办的初始状态与边界，以及本方案按“被替代”退出和归档分类。
- 本次仅授权保存第 4.1 节两份文件，不授权两个新待办的施工、当前方案关闭、归档移动、索引更新、空目录删除、命令、Git、安装、测试、发布或产品进程。
- 后续用户已另行授权关闭与归档；本方案状态据此记为“已被替代”，后续工作由当前待办入口中的两个独立待办承接。
