# DOC-LIFECYCLE-20260804：文档生命周期与收尾模板收口方案

- 待办 ID：`DOC-LIFECYCLE-20260804`
- 状态：`已完成`
- 治理路线：标准骨架日常检查后的结构收口
- 保存决定：用户于 2026-08-04 明确要求“保存方案”
- 当前阶段：用户于 2026-08-04 验收通过并授权关闭；活动待办已退出，方案与归档索引已经形成历史导航
- 主责边界：文档生命周期、方案收尾模板、当前待办历史来源表述及其结构接线

## 1. 初始检查范围与结论

### 1.1 冻结范围

本方案以 2026-08-04 的当前工作树文本为初始检查证据。检查先完整读取适用的根 `AGENTS.md`，列举全部 56 份 Markdown，并围绕以下结构证据形成有限集合：

- 用户、协作、文档和测试入口：`README.md`、`AGENTS.md`、`docs/README.md`、`SoftwareTesting/README.md`；
- 核心职责与活动状态：需求、设计、字段契约、待办、测试 Registry、ADR 和运维索引及其直接链接；
- 生命周期与历史：`archive/docs/README.md`、已完成计划索引、发布 workflow 历史索引，以及与本轮结构判断直接相关的既有骨架、治理和 T-DOC 方案；
- 测试治理：Protocol、Safety、T-DOC、T-PROJECT suite 入口、T-DOC 检测器和正反夹具；
- 机械与授权接线：四份活动 GitHub Actions workflow、项目源码结构检查器及当前 Skill 的两份标准 T-DOC 资产。

新增到初始集合的直接证据及理由：

- `archive/docs/plans/DOC-GOVERNANCE-20260803-测试入口与授权边界收口.md`：证明 T-PROJECT 未闭合证据已有独立待办承接；
- `archive/docs/plans/DOC-GATE-COMPAT-20260803-T-DOC标准资产与源码检查兼容方案.md`：证明项目选择标准 T-DOC 且曾完成资产同步；
- 当前 Skill 的两份 T-DOC 资产：判断标准路线的资产边界；
- `.github/workflows/` 与 `tools/verify_source.py`：核对机械门禁和停止发布的直接接线，不扩张为全项目安全认证。

### 1.2 七个结构维度

| 维度 | 结论 | 初始检查证据 |
| --- | --- | --- |
| 上下文与路线 | 满足 | 未发现 `CONTEXT-MAP.md`、根或嵌套 `CONTEXT.md`；既有已完成方案明确记录项目选择标准骨架路线 |
| 结构与职责 | 存在缺口 | 需求、设计、字段和测试职责已有唯一真源；`docs/README.md` 尚未承接稳定的文档生命周期和项目级收尾模板 |
| 入口与导航 | 满足 | 协作、用户、文档、测试和归档入口完整；核心文档、ADR、运维文档及活动 suite 均有适用的一至两跳入口 |
| 活动与历史生命周期 | 存在缺口 | 归档索引和当前承接关系完整；当前文档索引没有统一规定长期真源、当前手册、ADR、临时材料和活动方案的退出条件 |
| 待办、方案与记录 | 存在缺口 | `TEST-ENTRY-20260803` 的“待确认、零活动方案”数量关系合法，但其“已完成方案”表述没有区分已被替代的来源方案与当前尚无活动方案；项目级收尾模板缺失 |
| 测试治理结构 | 存在缺口，已有独立承接 | Registry、Protocol、Safety 和两个 suite 入口齐全；T-PROJECT 的 fixture、输出、断言和专项清理证据仍由 `TEST-ENTRY-20260803` 独立承接 |
| 机械门禁与授权边界 | 满足静态结构 | T-DOC、正反夹具、CI 接线和长期授权入口存在；完整静态读取未发现项目与当前 Skill 资产的文本差异，但未执行字节比较或动态验证 |

总体结论：继续保留当前标准骨架路径，不迁移目录或 Registry schema。当前不能宣称文档结构已经全部收口；本方案只处理共同根因是“当前文档索引缺少生命周期和收尾职责”的结构差异，不吸收 T-PROJECT 的实现与证据工作。

## 2. 证据不足与未覆盖范围

- 未获得 Git 命令授权，因此没有检查工作树状态、可发现 Git 历史中的待办 ID 唯一性或当前候选身份；保存前列举的当前和归档文件中未发现 `DOC-LIFECYCLE-20260804`，Git 历史唯一性仍待实施前补证。
- 未运行 T-DOC、自测试、源码检查、构建、产品测试或产品程序；所有动态验证当前均为 `not_run`。
- 两份项目 T-DOC 与当前 Skill 资产只完成解码文本的完整静态核对，没有执行逐字节比较。
- 未核对产品事实是否与实现一致，不评价测试覆盖、重复或经济性，不设计 T-PROJECT fixture，也不认证全项目安全。
- 没有遍历全部产品源码、全部测试实现或 Git 历史；发现会改变本方案结构结论的新直接证据时，普通实施任务必须停止并重新裁决受影响路径。

## 3. 差异、选项与用户决定

### 3.1 文档生命周期由谁承接

- 当前证据：`docs/README.md` 已是活动文档总入口，但只列核心文档、运维和归档入口，没有当前、临时、被替代、历史材料的共同退出规则。
- 选项 A：由 `docs/README.md` 增加项目级稳定生命周期规则，具体文档只保留自身职责。
- 选项 B：把规则分别写入每份活动文档。
- 推荐：选项 A。它保持单一结构入口，避免规则重复并允许方案、手册和 ADR 使用同一退出边界。
- 用户决定：同意按本轮建议保存集中方案；实际施工仍须由普通任务在重新冻结证据并取得文件修改授权后执行。

### 3.2 收尾与联合复核模板

- 当前证据：当前活动文档和归档中均未发现项目级稳定的六项收尾模板；既有历史方案各自使用不同收尾组织。
- 选项 A：在 `docs/README.md` 建立稳定模板，每份后续活动方案只实例化任务值。
- 选项 B：继续让每份方案自行发明收尾字段。
- 推荐：选项 A。它使完成证明、状态消费者、关联职责、验收停点和关闭边界可由普通任务独立理解。
- 用户决定：保存选项 A 的实施方案；保存不表示模板已经写入或通过验证。

### 3.3 `TEST-ENTRY-20260803` 的来源与边界

- 当前证据：该待办保持 `待确认` 且没有活动方案；既有 `DOC-GOVERNANCE-20260803` 被替代后把 T-PROJECT 证据工作交给此待办。
- 选项 A：只澄清来源方案已被替代并归档、当前待办尚无活动方案，不改变该待办目标或状态。
- 选项 B：把 T-PROJECT 的实现和文档工作并入本方案。
- 推荐：选项 A。T-PROJECT fixture、输出、断言和清理证据属于独立实现与测试职责。
- 用户决定：按选项 A 保存；本方案不得借澄清表述替代 `TEST-ENTRY-20260803` 的独立检查、方案或施工。

## 4. 目标与非目标

### 4.1 目标

1. 让 `docs/README.md` 成为当前文档分类、退出条件和活动方案收尾职责的唯一稳定入口。
2. 为后续活动方案提供可实例化、可验收、可关闭的六项收尾模板。
3. 使 `TEST-ENTRY-20260803` 的历史来源、当前状态和独立职责没有互相矛盾的暗示。
4. 保持当前标准路径、导航、Registry、suite 名称、授权规则和停止发布决策不变。

### 4.2 非目标

- 不同步需求、设计、字段、运维或产品事实，不修改 ADR 正文。
- 不设计或实现 T-PROJECT 的 fixture、隔离运行器、输出、断言或专项清理。
- 不修改 Registry schema、测试类别、T-DOC 标准资产、产品测试或产品代码。
- 不修改 workflow、发布策略、真实配置、数据库、媒体、凭据或仓库外运行现场。
- 不执行测试充分性审计、全项目安全认证或正式认证。

## 5. 路径与动作边界

### 5.1 保存阶段实际路径

| 动作 | 路径 | 边界 |
| --- | --- | --- |
| 修改 | `docs/已知问题与待做需求.md` | 新增唯一待确认待办并由该条目实际链接本方案；不改既有待办正文 |
| 新增 | `docs/方案/DOC-LIFECYCLE-20260804-文档生命周期与收尾模板收口.md` | 保存本集中方案；不进入施工状态 |

### 5.2 后续确定修改路径

这些路径只有普通实施任务重新冻结证据、确认无冲突并另行取得文件修改授权后才能修改：

- `docs/README.md`：增加稳定的文档生命周期规则和项目级收尾模板；不复制各核心文档正文。
- `docs/已知问题与待做需求.md`：澄清 `TEST-ENTRY-20260803` 的被替代来源方案、当前零活动方案状态和独立职责；施工开始时按实际授权更新本待办状态。
- 本方案：只记录后续实际授权、实施结果、验证结果、验收决定和关闭状态，不把保存时快照改写成当前结果。

### 5.3 可能修改与条件关闭路径

下列路径不是当前保存或施工的默认写入范围，只在完成条件成立、用户验收并另行授权关闭时处理：

- `archive/docs/plans/DOC-LIFECYCLE-20260804-文档生命周期与收尾模板收口.md`：由活动方案完整移动形成历史记录；
- `archive/docs/plans/README.md`：登记已完成或其他经用户确认的最终分类；
- `archive/docs/README.md`：登记历史职责和当前承接真源；
- `docs/已知问题与待做需求.md`：移除已经退出活动生命周期的本待办及方案链接；
- `docs/方案/`：仅在活动方案退出且目录确实为空时，按项目规则另行判断是否需要精确删除授权。

### 5.4 明确不修改与停止条件

- `SoftwareTesting/doc_consistency/test_doc_consistency.py` 和 `test_doc_consistency_rules.py` 仅作为验证对象；若逐字节比较发现与施工时当前 Skill 资产冲突，停止并由用户另行决定，不自动覆盖。
- `SoftwareTesting/project/`、`docs/软件测试.md`、Protocol、Safety、产品代码、产品测试、workflow、ADR 和归档正文不属于本方案施工目标。
- 实施前必须重新冻结全部拟改文件。发现用户已有修改、活动方案重复、待办 ID 冲突、生命周期规则已经由其他任务承接，或覆盖后无法可靠恢复时，停止受影响路径。
- 文件修改、Shell、Git、构建、测试、安装、commit、push、PR、真实数据、产品进程、删除和关闭分别授权；本次保存不推定其中任何一项。

## 6. 实施阶段交接顺序

1. 重新读取适用 `AGENTS.md`、本待办、本方案和全部拟改文件，复核保存后的结构漂移及共享文件状态。
2. 补证候选 ID 的 Git 历史唯一性；未获 Git 命令授权时保持未验证，不以推断替代。
3. 取得普通实施任务所需的明确文件修改授权后，先在 `docs/README.md` 建立稳定生命周期和收尾职责。
4. 在不改变 T-PROJECT 目标与状态的前提下，澄清 `TEST-ENTRY-20260803` 的来源和当前活动方案状态。
5. 完成静态复核后，在另行取得命令与测试授权时执行本方案的普通验证；任何候选变化都重新判断受影响项。
6. 向用户报告实际实施和验证结果并停在验收点；没有验收与关闭授权时保持活动待办和方案。
7. 只有用户验收并授权关闭后，才移动方案、更新归档索引、退出活动待办并对最终状态重新验证。

## 7. 完成条件

- `docs/README.md` 明确区分长期当前真源、当前可执行手册、ADR、活动待办、活动方案、临时材料和历史材料，并为每类给出适用入口与退出条件。
- `docs/README.md` 提供稳定的“收尾与联合复核”模板，覆盖本方案主责与完成证明、关闭时的状态消费者、关联方案与共享文件、联合只读复核触发条件、验收停点、关闭边界与未运行验证。
- `TEST-ENTRY-20260803` 的正文明确说明来源方案已被替代并归档、当前没有活动方案，且不暗示其实现工作已经完成。
- 当前用户、协作、文档、测试和归档导航继续成立；核心文档、ADR、运维和活动 suite 的可达性不退化。
- T-PROJECT 的实现与证据缺口继续由原待办独立承接，本方案不宣称测试治理整体闭环。
- 获得独立命令授权后，计划中的普通验证对实际候选取得可解释结果；未运行项继续明确标记 `not_run`。
- 用户验收并授权关闭后，本待办与方案退出活动入口，归档方案和两个归档索引形成一致导航，并对关闭后的最终状态重新验证。

## 8. 验证要求与证据语义

- 测试层级：普通验证；不自动提升为全量测试或正式认证。
- 验证影响域：文档分类与退出条件、活动待办和方案生命周期、项目级收尾模板、两跳导航、归档登记、T-DOC 标准资产边界，以及与 `TEST-ENTRY-20260803` 共享的待办入口。
- 具体验证项：静态复核 `docs/README.md` 的生命周期和六项模板、全部受影响相对链接及两跳可达性；逐字节比较项目中的两份 T-DOC 源码与施工时当前 Skill 对应资产；运行 T-DOC 正反规则夹具和目标项目门禁；确认未修改 T-PROJECT suite、Registry、Protocol、Safety、ADR、workflow、产品代码或产品测试；用户授权关闭后，再对方案移动、待办退出和归档索引完成后的最终状态执行 T-DOC。

保存方案时，上述动态项目均为 `not_run`。初始检查证据是历史快照，完成条件不是实施结果；只有实际文件写入后才能记录实施结果，只有对应验证、验收、移动和最终门禁实际完成后才能记录关闭结果。任何受影响 Markdown、活动待办、方案、归档索引、T-DOC 资产或候选身份变化，都会使此前相关验证失效并要求重新判断或重跑。

## 9. 授权、恢复与风险控制

- 用户于 2026-08-04 明确授权实施 `DOC-LIFECYCLE-20260804`，随后单独授权本方案的普通 T-DOC 验证；用户验收通过后又明确授权关闭，覆盖第 5.3 节的活动退出、完整归档和索引同步。Git、构建、产品程序、真实数据、内容删除等操作仍未获授权。
- 当前文件读取来自实际工作树，但 Git 状态未知。普通实施任务必须保护用户已有修改，不覆盖冲突内容。
- 移动或覆盖前确认内容能够从 Git 或其他可靠来源精确恢复；未跟踪、已有未提交修改或其他不可恢复内容必须先建立恢复方式。
- 归档移动必须完整保留方案正文并同步更新引用；内容丢弃、目录清理、命令、Git 和外部副作用继续分别授权。
- 发现结构结论需要依赖产品事实、测试实现判断或真实语义取舍时，停止并形成独立交接，不扩大本方案。

## 10. 职责外交接

- `已完成`：文档生命周期、项目级收尾模板、待办来源澄清、活动退出和归档导航均已按授权完成。
- `待实现或验证`：`TEST-ENTRY-20260803` 继续承接 T-PROJECT fixture、输出、断言和专项清理证据；本方案只保证不覆盖该职责。
- `待事实同步`：当前未发现需要由本方案处理的产品事实同步；若施工中发现，交给相应当前真源任务。
- `待语义决策`：当前没有需要在保存阶段裁决的产品语义；若实施发现文档分类无法由现有职责证据判断，保持待决并请求用户选择。

## 11. 收尾与联合复核

- 本方案主责与完成证明：以 `docs/README.md` 已形成稳定生命周期和六项收尾模板、`TEST-ENTRY-20260803` 表述不再混淆来源与当前状态、适用普通验证取得可解释结果为主责完成证明。
- 关闭时的状态消费者：`docs/已知问题与待做需求.md`、`docs/README.md`、`archive/docs/plans/README.md`、`archive/docs/README.md`，以及对最终生命周期状态进行检查的 T-DOC。
- 关联方案、共享文件与对方职责状态：关联职责是尚无活动方案的 `TEST-ENTRY-20260803`；双方仅共享当前待办入口，本方案不等待或替代其 T-PROJECT 实现职责，关闭时必须保留其真实状态。
- 联合只读复核触发条件：本方案达到自身完成条件且 `TEST-ENTRY-20260803` 已创建方案或改变共享待办入口时，关闭前对两项职责和共享文件做一次只读复核；若对方仍未变化，则只复核本方案没有改写其状态或目标。
- 验收停点：普通实施任务完成文件写入和已授权验证后已向用户报告实际证据；用户于 2026-08-04 明确确认验收通过并授权关闭。
- 关闭边界与未运行验证：活动退出、完整归档和索引登记已经执行；关闭候选冻结后的最终 T-DOC 由本次关闭任务交付报告。没有运行 Git、构建、产品程序、真实数据或内容删除操作，也不证明内容一致性、测试充分性、全项目安全或整体治理已经闭环。

## 12. 实施与关闭结果（2026-08-04）

- 用户明确授权实施本方案的确定文件修改，随后单独授权 T-DOC 规则夹具、目标门禁和两份标准资产逐字节比较；普通验证通过后，用户明确确认验收通过并授权关闭。
- `docs/README.md` 已增加长期真源、当前手册、ADR、活动待办与方案、临时材料、历史材料和检查记录的生命周期规则，并建立项目级六项收尾模板。
- 实施期间 `docs/已知问题与待做需求.md` 曾把本待办转为 `实施中`；关闭时本待办及活动方案链接已退出，`TEST-ENTRY-20260803` 继续保持 `待确认`，其目标、状态和零活动方案关系均未改变。
- 本方案已完整移动到 `archive/docs/plans/`，并由已完成计划索引和文档归档索引登记；没有修改 T-PROJECT suite、Registry、Protocol、Safety、T-DOC、ADR、workflow、产品代码或产品测试。
- T-DOC 正反规则夹具实际运行 27 项，全部通过。
- 写入验证记录前的目标项目门禁实际通过，结果为 0 warning；验证记录写入后的目标门禁结果只在本次任务交付中报告，不反写本方案，避免为记录结果再次改变验证候选。
- 两份标准资产逐字节比较均一致：`test_doc_consistency.py` 为 32651 字节、SHA-256 `6CF42DA26279A8BA01B7DAC196F852D855B2EAA81D829CD9048CC498F649170B`；`test_doc_consistency_rules.py` 为 21029 字节、SHA-256 `E11DA839658EF7DF2D242894BF107D4E80E60806B8C02EEB29A3F907E734B438`。
- 关闭前联合只读复核确认关联职责没有新方案或共享入口变化，本方案没有改写 `TEST-ENTRY-20260803` 的目标或状态。
- 没有运行 Git、构建、产品程序、真实数据或内容删除操作；关闭候选冻结后的最终 T-DOC 结果只在本次关闭任务交付中报告，避免为记录结果再次改变已验证候选。
