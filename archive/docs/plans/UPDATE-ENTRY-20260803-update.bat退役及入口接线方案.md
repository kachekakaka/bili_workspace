# UPDATE-ENTRY-20260803：update.bat 退役及入口接线方案

- 待办 ID：`UPDATE-ENTRY-20260803`
- 状态：已完成
- 方案类型：实施主责、骨架复核
- 治理路线：标准骨架日常检查后的活动入口退役
- 来源：2026-08-03 `project-doc-skeleton` 独立只读检查
- 当前授权：已获裁决记录、已确认路径修改、`update.bat` 删除、普通验证、临时 Git 索引模拟与清理、生命周期关闭及精确空目录删除授权

## 1. 目标与非目标

### 1.1 目标

1. 使根目录 `update.bat` 退出当前 Windows 用户入口，并在确认可恢复后按单独删除授权退役。
2. 同步全部已确认的活动文档入口、当前结构说明、源码边界和入口机械断言，避免退役后继续要求或推荐该文件。
3. 保留现有手动源码更新路径、`start.bat` 启动入口和 `verify.bat` 部署自检入口，不为退役目标创建替代包装脚本。
4. 由实施职责处理文件删除、事实同步和直接消费者；`project-doc-skeleton` 只复核入口、导航、生命周期、结构机械断言和授权边界。

### 1.2 非目标

- 不重新设计数据目录、配置目录、测试入口、发布流程或恢复流程。
- 不迁移文档目录、Registry schema、suite、SAFETY、T-DOC 实现或活动发布 workflow。
- 不核对其他产品事实，不评价测试覆盖经济性，不执行全项目安全认证。
- 不恢复未来正式发布，也不创建 tag、Release、正式镜像或新的更新自动化。

## 2. 施工前证据与当前结果

### 2.1 入口行为与现有承接

- `update.bat` 在施工前会检查工作区、执行 Git 快进更新、调用 `verify.bat`，并在成功后启动 `start.bat`；它是带 Git、测试和产品进程副作用的可执行运维入口，不是单纯导航文件。
- 根 [`README.md`](../../../README.md) 已分别提供现有手动源码更新路径、`start.bat` 启动入口和 `verify.bat` 自检入口。退役 `update.bat` 不需要发明新的更新流程或迁移根 README。
- [`docs/运维/发布与回滚流程.md`](../../../docs/运维/发布与回滚流程.md) 已同时保存 `update.bat` 推荐入口和现有手动路径；后续只退出前者并保留后者，不借此重写其余运维事实。

### 2.2 已确认的直接消费者

- 入口和结构说明：[`docs/README.md`](../../../docs/README.md)、[`scripts/README.md`](../../../scripts/README.md)、[`docs/设计文档.md`](../../../docs/设计文档.md)。
- 运维与配置事实：[`docs/运维/发布与回滚流程.md`](../../../docs/运维/发布与回滚流程.md)、当时存在的 `config/README.md`（现已随旧 Windows 链退役）。
- 源码边界和结构机械断言：[`tools/verify_source.py`](../../../tools/verify_source.py)、当时存在且现已退役的 `tests/test_repository_layout.py` 与 `tests/test_integrated_runtime.py`（后者职责已由 T-LAUNCHER 接替）。
- 条件式产品源码引用：[`app/build_info.py`](../../../app/build_info.py) 在施工前会在文件存在时把三个根入口纳入源码指纹；用户已裁决并实施移除其中的退役名称。
- 施工前的 [`DOC-GOVERNANCE-20260803` 活动方案](DOC-GOVERNANCE-20260803-测试入口与授权边界收口.md) 曾以 Markdown 链接指向 `update.bat`；该方案现已按“已被替代”退出活动区并完成归档登记。

### 2.3 当前未发现的迁移要求

- 当前活动 Registry 仍由 T-DOC 和 T-PROJECT 承接；本次退役不新增测试项，也不改变执行类别。
- T-DOC 检查文档路径、链接、导航和生命周期，不硬编码 `update.bat`；项目现有源码边界和定向测试承担根入口断言。
- 当前直接引用扫描没有发现活动 workflow、默认配置文件或其他启动脚本调用 `update.bat`；历史工作记录中的文字只保留历史证据。

## 3. 处理选项、推荐与用户决定

### 3.1 方案主责

- 选项 A：由一份实施主责方案统一承接删除、直接事实同步和机械消费者调整，骨架只做独立结构复核。
- 选项 B：拆成骨架施工方案和另一份实现方案；这会拆散同一入口的退出闭包，并使单一待办的完成责任不清。
- 推荐：选项 A。它保留一个待办、一份活动方案和一个完成结论，同时不把产品或运维事实裁决伪装成骨架工作。
- 用户决定：采用选项 A；本方案不指定由哪个 Skill、agent 或工具施工。

### 3.2 `app/build_info.py` 条件式引用

- 选项 A：移除退役名称，使活动源码中不再保留该入口引用；需要产品源码修改授权和相应实现证据。
- 选项 B：保留条件式引用，并明确其不会要求文件存在；这样不阻断运行，但活动源码仍保留退役名称。
- 推荐：选项 A，以保持“直接退役”和活动引用闭包一致。
- 用户决定：采用选项 A；实施阶段移除 `app/build_info.py` 中的退役名称。

## 4. 目标路径与动作分类

### 4.1 本次保存阶段

- `docs/已知问题与待做需求.md`：为待办增加本方案的实际链接，状态保持 `待确认`。
- `docs/方案/UPDATE-ENTRY-20260803-update.bat退役及入口接线方案.md`：新增本方案并记录证据、边界和待决项。

本次不修改其他文件。

### 4.2 后续实施阶段的已确认路径

| 路径 | 候选动作 | 边界 |
| --- | --- | --- |
| `update.bat` | 删除 | 先证明可恢复并取得独立删除授权；不得把运行该入口当作退役验证 |
| `docs/README.md` | 修改 | 只同步根目录 Windows 入口结构 |
| `scripts/README.md` | 修改 | 只退出已退役入口说明，保留其他脚本职责 |
| `docs/运维/发布与回滚流程.md` | 修改 | 退出旧推荐入口，保留既有手动路径；不重设计发布或回滚流程 |
| `docs/设计文档.md` | 修改 | 只同步当前 Windows 入口清单，不扩展实现事实审计 |
| `config/README.md` | 修改 | 只移除已不存在入口的模板同步声明，不改配置契约 |
| `tools/verify_source.py` | 修改 | 只调整根入口的必要文件、允许集合及直接结构边界 |
| `tests/test_repository_layout.py` | 修改 | 只调整可分离的根入口、允许文件和退役入口接线断言 |
| `tests/test_integrated_runtime.py` | 修改 | 只调整可分离的退役入口接线断言，不改其他运行资产行为 |
| `app/build_info.py` | 修改 | 按用户裁决移除源码指纹集合中的退役名称，不改指纹算法 |

表中路径是当前证据确认的受影响范围；施工前仍需重新冻结，不得因文件出现在表中而整份放行修改。

### 4.3 已裁决路径

- `app/build_info.py`：用户已选择第 3.2 节选项 A，现由候选路径转为已确认修改路径。

### 4.4 生命周期路径

- `docs/已知问题与待做需求.md` 和本方案：施工开始、验收及关闭时按各自阶段更新。
- `archive/docs/plans/`、`archive/docs/plans/README.md`、`archive/docs/README.md`：只有用户验收并另行授权关闭后才处理本方案归档。
- `DOC-GOVERNANCE-20260803` 的退出、移动和归档属于其自身方案，不并入本方案施工。

### 4.5 明确不改

- `README.md`、`start.bat`、`verify.bat`：当前证据已提供独立更新、启动和验证承接；没有新证据时保持不变。
- `docs/软件测试.md`、`SoftwareTesting/README.md`、`SoftwareTesting/PROTOCOL.md`、`SoftwareTesting/SAFETY.md`、`SoftwareTesting/doc_consistency/`、`SoftwareTesting/project/`：不因本次退役迁移或改写。
- 活动发布 workflow、归档历史正文、数据目录、配置 schema、ADR、真实配置、数据库、媒体和凭据均不在范围内。

## 5. 动作边界与交接

- 实施职责负责 `update.bat` 删除、产品源码待决项、当前事实说明和直接机械消费者的一致变更，并对这些内容的正确性负责。
- 骨架复核只判断路径是否完整、职责是否分离、活动入口是否退出、手动承接是否仍可达、生命周期是否一致，以及结构机械断言是否与目标路径一致。
- 修改同一测试文件时，只处理可与产品行为可靠分离的入口和自动化接线断言；发现无法分离的产品断言时停止该文件并交接，不扩大授权。
- 施工前重新搜索直接引用、默认接线、启动调用、自动化触发和运维入口。出现新的部署策略、产品行为或恢复取舍时停止，不先删除上游入口。
- 当前和归档中的历史文字不因关键词命中而自动修改；只有活动消费者或生命周期入口进入施工范围。

## 6. 副作用效果摘要

- **会发生**：本轮获得施工和删除授权后，目标脚本已退出仓库活动入口，已确认的活动说明及直接机械消费者已同步调整。
- **不会发生**：本轮没有运行 `update.bat`、Windows 完整自检、产品、联网、安装或发布，也没有修改方案明确范围之外的项目文件。
- **既有状态**：施工前 `update.bat` 若被人工运行会触发 Git 更新、部署自检和产品启动；施工后该入口已不存在。

## 7. 施工前置条件

1. 用户已裁决第 3.2 节的产品源码引用选项。
2. `DOC-GOVERNANCE-20260803` 已按其自身生命周期退出活动区并完成归档登记。
3. 已在独立只读 Git 授权下补证待办 ID 的可发现历史唯一性，没有发现重复。
4. `update.bat` 受 Git 跟踪并存在五个历史提交；工作树差异忽略行尾后与已提交内容一致，删除前已证明可恢复。
5. 已重新读取适用 `AGENTS.md`、本方案、待办状态和全部拟改文件，并在当前未提交修改上使用局部补丁，未覆盖其余改动。
6. 施工开始时已分别取得已确认路径修改、`update.bat` 删除和只读 Git 补证授权；普通验证、临时索引模拟、关闭和空目录清理在后续阶段分别获得授权。
7. 待办和方案已在差异裁决及施工授权后转为 `实施中`。

## 8. 完成条件

- `update.bat` 已按独立删除授权退出活动源码树，且删除前可恢复性有明确证据。
- 活动入口、当前结构说明、运维说明和配置说明不再要求或推荐 `update.bat`；既有手动更新、`start.bat` 和 `verify.bat` 承接保持不变。
- 根目录必要文件、允许文件和入口接线的机械断言不再要求读取或存在 `update.bat`。
- `app/build_info.py` 已按用户裁决执行修改或明确保留，并有对应实现证据；不得以未处理状态关闭待办。
- 当前活动 Markdown 不存在指向已删除目标的断链；历史文字保持历史分类，不冒充当前入口。
- Registry schema、suite、SAFETY、T-DOC、活动发布 workflow、数据目录和发布停止决策没有因本次退役发生迁移。
- 用户验收实施结果并另行授权关闭后，待办退出活动入口，本方案归档且索引与当前承接一致；未获关闭授权时继续保持活动状态。

静态实施项和普通验证已完成；用户已验收并授权关闭，本待办已退出活动入口，本方案已归档。

## 9. 验证要求

- 测试层级：普通验证。
- 验证影响域：根目录 Windows 入口集合、活动文档中的入口与运维承接、源码边界、入口接线断言、活动方案生命周期和 Markdown 链接。
- 具体验证项：静态复核全部直接引用和目标路径分类；复核 `update.bat` 不存在且 `start.bat`、`verify.bat` 与现有手动更新承接仍在；运行适用 T-DOC 规则夹具、目标项目门禁并比较标准资产；运行项目现有源码边界检查及受影响的定向结构测试。只有用户另行选择时才提升为全量测试或正式认证。

普通验证结果：两份 T-DOC 与当前 Skill 标准资产逐字节一致；规则夹具 27/27 通过；施工状态下目标 T-DOC 通过且为 0 warning；项目源码边界检查通过。受影响定向测试首次运行 7 项通过、1 项因真实 Git 索引仍保留待提交删除的 `update.bat` 而失败；在用户授权的临时索引副本中登记该删除后，失败项通过，因此定向集合最终 8/8 通过。真实 `.git/index` 前后 SHA-256 一致，临时索引和锁文件已清理。

关闭后的最终 T-DOC 在本方案归档、索引更新及精确空目录删除后执行，结果在本次任务交付中报告，以确保验证对象是最终文档状态。

## 10. 用户决定与授权状态

- 用户已确认直接退役 `update.bat`，保留现有手动更新承接，不另行设计替代入口。
- 用户已确认采用“实施主责、骨架复核”边界，并授权保存本方案及待办链接。
- 用户已选择第 3.2 节选项 A，并授权已确认路径修改、`update.bat` 删除、`DOC-GOVERNANCE` 关闭归档和只读 Git 补证。
- 用户另行授权普通验证、只读 Git 枚举、临时 Git 索引模拟与临时文件清理、UPDATE 方案关闭，以及归档后精确删除空的 `docs/方案` 目录。
- 本次未获得安装、真实 Git 索引修改、commit、push、PR、发布或产品进程授权；相关操作不得从现有授权推定。
