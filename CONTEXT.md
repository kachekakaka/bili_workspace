# bili_workspace

本上下文定义私人 Bilibili 搜索、下载、任务管理与媒体库在 Windows 和 NAS 运行、交付时的核心语言。

## Language

### Windows 运行

**Windows 启动器**：
本项目在 Windows 上的单文件桌面应用，是本机运行和 Docker 导出的用户入口。
_Avoid_：Windows 服务、EXE 后台服务

**Windows 打包快照**：
由维护者明确触发、从干净且已提交的 Git `HEAD` 构建、自检并确认后纳入仓库的最近一份 Windows 启动器；它记录完整源码 commit，可以落后于 `main`。
_Avoid_：当前源码 EXE、最新源码 EXE

**Windows 候选构建**：
为证明某一源码候选能够生成单文件 Windows 启动器并通过自检而创建的临时 EXE；它不是 Windows 打包快照，也不进入仓库或上传。
_Avoid_：试发布、候选快照、临时发布物

**控制根**：
与某一份 Windows 启动器绑定、只保存可重建控制状态和资源的目录；其内容不属于业务数据。
_Avoid_：启动器数据目录、EXE 数据目录

**数据根**：
由 Windows 用户在源码仓库和控制根之外选择、在启动器更换后仍保留配置、运行状态和永久媒体的持久化根。
_Avoid_：运行目录、仓库数据目录

### 网络访问

**本机模式**：
服务只监听回环地址，因此只有同一台 Windows 主机能够直接访问的网络模式。
_Avoid_：离线模式

**局域网服务器模式**：
用户显式允许同一局域网内其他设备访问服务的网络模式；它不表示公网可达，也不放宽认证与传输安全要求。
_Avoid_：局域网模式、服务器模式、公开模式

### Bilibili 内容

**网站用户**：
登录本网站并拥有本人任务、会话和权限的本地账号；它不同于被查找投稿的 Bilibili 账号。
_Avoid_：用户、Bilibili 用户

**Bilibili 登录**：
由管理员维护、供服务端代表所有网站用户访问 Bilibili 的共享平台会话；它不是网站用户身份，也不是当前查找的 UP 主。
_Avoid_：用户登录、UP 主登录

**UP 主**：
在 Bilibili 发布视频投稿、可作为投稿查找目标的账号；它不是本网站的用户身份。
_Avoid_：用户、作者、网站用户

**UID**：
Bilibili 为 UP 主分配的稳定数字标识；名称只用于查找和展示，不作为身份标识。
_Avoid_：用户 ID、用户名、mid

**UP 主定位符**：
能够无歧义解析为一个 UID 的纯数字 UID 或 Bilibili UP 主主页链接；名称和关键词不属于 UP 主定位符。
_Avoid_：用户名、搜索词、主页名称

**视频投稿**：
UP 主公开发布、由 BV 号标识并可进入现有下载链路的视频作品；不包括动态、专栏、音频、直播和收藏夹内容。
_Avoid_：用户作品、全部作品

**自动最高画质**：
下载时从当前 Bilibili 登录实际可用的档位中选择最高档；它不绕过最低清晰度门槛，也不表示用户指定了某个固定档位。
_Avoid_：固定画质、指定画质

**最低清晰度门槛**：
用户可接受画质的下限；即使已经选择自动最高画质，最高可用档位仍低于门槛时任务也必须失败。
_Avoid_：目标画质、自动降级

**设备下载记录**：
普通网站用户的浏览器完整接收设备导出后形成、按网站用户和作品长期保留的事实；准备完成或传输中断不会产生该记录。
_Avoid_：成功任务、可下载导出、任务历史

**待领取设备导出**：
服务端下载任务已经完成、仍可由所属网站用户领取的设备导出；其文件可以在领取时完成必要打包。它不是已下载作品，并在领取、过期或放弃前阻止同作品重复创建任务。
_Avoid_：已下载、下载完成

**已下载作品**：
对管理员是共享媒体库中已有有效文件的作品，对普通网站用户是本人已有设备下载记录的作品；该状态只参与筛选、不禁止重新下载，且不向其他普通网站用户扩散。
_Avoid_：全站已下载、他人已下载

**符合条件的投稿**：
同时通过当前标题二级筛选和当前网站用户本地状态筛选的视频投稿；用户尚未勾选不表示该投稿不符合条件。
_Avoid_：不要的作品、未选择作品

**可下载候选**：
对当前网站用户而言符合条件、且不存在同作品活动任务或待领取设备导出的视频投稿；包含多个分 P 时仍以整个视频投稿作为一个候选。
_Avoid_：可见作品、单个分 P

**搜索上下文**：
由搜索模式及其目标关键词或目标 UP 主共同确定、承载跨页选择的范围；翻页、排序和标题筛选不建立新的搜索上下文。
_Avoid_：当前页面、筛选结果

**UP 主全量入库**：
管理员针对单一 UID，将发起时已经发布的全部公开视频投稿作为发现范围、一次性补齐到共享媒体库的操作；当前标题筛选不缩小这一范围，作业固定按发布时间倒序遍历，后续新投稿不会自动纳入。媒体库已有、存在活动任务或已有删除历史的投稿保持现状，既有失败或已取消任务不阻止再次创建。
_Avoid_：全部下载、全部作品、全量筛选结果

**全量入库作业**：
一次 UP 主全量入库在当前服务运行期内的非持久后台协调过程。它串行、低优先级地发现投稿并增量创建独立下载任务，将预期跳过与单条失败分别记录；取消只终止后续处理。
_Avoid_：批量下载提交、可恢复作业、批量任务

### 任务生命周期

**批量下载提交**：
网站用户一次确认的多个可下载候选组成一个任务创建请求；提交前校验必须全部通过，才能为所有候选创建彼此独立的任务。
_Avoid_：部分创建、尽力提交

**活动任务**：
尚未进入终态的下载任务，包括排队中、下载中、正在暂停和正在取消的任务。

**正在暂停**：
暂停请求已经被接受、但关联执行尚未完全停止的活动任务阶段；只有停止完成后，任务才进入任务暂停终态。
_Avoid_：任务暂停

**正在取消**：
取消请求已经被接受、但关联执行尚未完全停止的活动任务阶段；只有停止完成后，任务才进入已取消终态。
_Avoid_：已取消

**任务重试**：
终态任务保留原任务身份、拥有者和最初创建时间，清空上一次执行状态、结果与日志后重新开始；成功、跳过、失败、取消和暂停任务均可重试，只保留最新一次执行，不建立尝试历史。
_Avoid_：新任务、重置创建时间、重试历史

**任务暂停**：
任务执行已经停止、但仍保留原任务身份并允许继续的终态；它不同于用户放弃该任务的取消。
_Avoid_：任务取消

**任务继续**：
已暂停任务保留原任务身份重新开始执行；除非底层能力另有明确保证，它不表示从既有文件字节位置续传。
_Avoid_：断点续传

### 媒体库

**作品删除**：
用户将作品移出媒体库，并按本次选择保留或移除关联媒体文件的操作；只有所选范围全部完成才能称为成功，部分清理不算已删除。
_Avoid_：尽力删除、部分成功

**批量作品删除**：
由多个彼此独立的作品删除组成；每个作品分别完整成功或失败，批次明确列出各项结果，不承诺整个批次全有或全无。
_Avoid_：批次原子删除、整批回滚

### 测试验证

**真实测试凭据源**：
由维护者明确授权、只为真链测试提供 Bilibili 登录和非秘密测试目标的数据根；真链测试不得在其中运行产品或修改其业务状态。
_Avoid_：真实测试数据根、日常数据根、任意登录目录

**真链测试运行**：
使用真实测试凭据源创建、由单次测试独占的隔离运行；其业务状态允许被测试破坏，但不取得修改凭据源的权限。
_Avoid_：真实测试凭据源、日常运行、共享测试环境

**Bilibili 真链冒烟**：
在真链测试运行中使用实际 Bilibili 登录与网络，短时、有界地验证当前源码或受影响候选的关键成功链路；它不替代失败、并发和异常边界的模拟测试。
_Avoid_：完整测试、生产验收、全量真实回归

### Docker 交付

**Docker 导出三件套**：
Windows 启动器针对同一构建身份生成的 Docker 镜像 tar、SHA-256 校验文件和身份清单。
_Avoid_：下游交付物、Docker 产物
