# V0.7.0 前端兼容文件清单

> 状态：PR 6 搜索页正式迁移  
> 依据：[V0.7.0 前端结构整理方案](V0.7.0_前端结构整理方案.md)与[方案 Review](V0.7.0_前端结构整理方案_REVIEW.md)

本清单记录 V0.6.2 兼容文件的唯一删除阶段和当前状态。PR 6 完成后，所有页面 renderer 均为正式 ES Module；PR 7 只收口 Core 兼容全局、版本检查和 CSS。

## 仍在生产入口或仓库中的兼容资产

| 当前文件 | 当前职责 | 正式替代/删除阶段 |
|---|---|---:|
| `web/assets/app.js` | 已停止加载的旧 App Shell 源文件 | PR 7 |
| `web/assets/enhancements-core.js` | 临时 Legacy Bridge 状态同步；不再拥有页面 renderer | PR 7 |
| `web/assets/browser-version.js` | 前端/服务版本提示 | PR 7（并入 App Shell） |
| `web/assets/app.css` | 旧基础样式 | PR 7（拆入语义 CSS 层） |
| `web/assets/enhancements.css` | 当前共享页面/组件样式 | PR 7 |
| `web/assets/ui-v062.css` | V0.6.2 UI 覆盖 | PR 7 |

## 已按阶段删除

| 文件 | 替代实现 | 完成阶段 |
|---|---|---:|
| `web/assets/enhancements-polish.js` | `pages/dashboard.mjs` 等正式页面 | PR 3 |
| `web/assets/enhancements-ui-v062.js` | Users / Groups / Account 正式页面与应用内对话框 | PR 3 |
| `web/assets/enhancements-ui-v062-settings.js` | `pages/settings.mjs` | PR 3 |
| `web/assets/ui-v062-settings.css` | Settings 原生 `details` 和现有语义样式 | PR 3 |
| `web/assets/enhancements-task-actions.js` | `pages/tasks.mjs` 单项/批量动作与应用内确认 | PR 4 |
| `web/assets/enhancements-tasks.js` | `pages/tasks.mjs` + `core/task-stream.mjs` | PR 4 |
| `web/assets/enhancements-tag-palette.js` | `pages/library.mjs` 的确定性标签颜色策略 | PR 5 |
| `web/assets/enhancements-library.js` | `pages/library.mjs` 单一作品库 renderer | PR 5 |
| `web/assets/enhancements-ui-recovery.js` | 正式 Library 筛选 chip 与 Dashboard 模块布局 | PR 5 |
| `web/assets/enhancements-library-overlay.js` | Library 分组、标签、播放器和移动操作 | PR 5 |
| `web/assets/enhancements-catalog-v2.css` | 样式已吸收到 `enhancements.css`，等待 PR 7 语义拆分 | PR 5 |
| `web/assets/enhancements-search.js` | `pages/search.mjs` + `core/search-policy.mjs` | PR 6 |

## 固定门禁

- 不得新增 `enhancements-v070-*` 或 `ui-v070-*` 文件；
- 新 Core/组件不得读取 `window.BiliEnhancements` 或 `window.BiliLegacyApp`；
- 页面不得使用 `MutationObserver`、捕获阶段抢占、原生 `prompt()` 或 `confirm()`；
- 每个旧 renderer/overlay 只能在同一 PR 已有正式替代实现和行为测试时删除；
- `window.BiliLegacyApp` 与 `window.BiliEnhancements` 必须在 PR 7 删除；
- 最终唯一允许保留的第三方全局是 `window.QRCode`。
