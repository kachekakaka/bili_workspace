# UP 主全量入库使用当前运行期低优先级作业

UP 主全量入库不复用最多 100 项且全有或全无的批量下载提交，而由管理员确认后建立当前服务运行期内、系统单并发的低优先级作业，固定按发布时间倒序逐页发现，并在保留手工队列容量时增量创建独立下载任务。作业在服务重启后不恢复，取消不撤销已经创建的任务，单条失败记录后继续且分页持续失败时停止在原页；这样在支持超过 100 条投稿的同时，避免播放量排序漂移、持久化调度、并发触发平台限流、挤占手工提交或产生连带取消。
