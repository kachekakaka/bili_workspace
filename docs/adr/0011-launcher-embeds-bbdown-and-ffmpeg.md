# Windows 启动器离线内置 BBDown 与 FFmpeg

旧便携运行链退役后，单文件 Windows 启动器直接内置固定版本且有来源与摘要记录的 BBDown 和 FFmpeg，不在首次启动时联网下载；使用前将工具校验并解包到 EXE 同级的版本化资源目录。Bilibili 会话凭据 `BBDown.data` 仍由所选仓库外数据根拥有，不进入 EXE 或控制根。项目接受由此增加的 EXE 体积，以换取离线可用和工具身份确定；候选若超过 Git 托管单文件边界，必须停止重新决策，不能静默改成在线下载、Git LFS、拆分包或外部发布。
