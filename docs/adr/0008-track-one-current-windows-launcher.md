# 仓库跟踪唯一的当前 Windows 启动器

项目决定在 `dist/` 中跟踪唯一一个与当前源码及内置构建上下文严格对应的 Windows 启动器 EXE，以便日常运行和下游 Docker 打包不要求用户先准备 Python、Git 或重新构建启动器。构建流程必须先生成候选、校验版本与资源哈希并完成自检，全部通过后才替换规范文件；其他 EXE 和中间产物仍被忽略。该文件不恢复版本 tag、GitHub Release、GHCR 或其他正式发布能力；若产物超过 Git 托管单文件边界，实施必须停止重新决策，不擅自引入 Git LFS、拆分包或外部发布渠道。
