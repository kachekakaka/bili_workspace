# Windows 启动器控制根与 EXE 同级

项目不把启动器设置、版本化资源或临时作业放入 `%LOCALAPPDATA%` 等机器公共位置，而是以 EXE 所在目录作为可移植控制根：`launcher.json`、`resources/` 和 `work/` 均与 EXE 同级。`launcher.json` 只记住所选仓库外数据根及启动参数；文件丢失时按首次启动重新选择数据根，不迁移、不删除也不影响实际业务数据。该选择要求 EXE 位于当前用户可写目录，并接受复制到新目录会形成独立控制实例，以换取配置位置直观、可携带且不在公共目录留下隐式状态。
