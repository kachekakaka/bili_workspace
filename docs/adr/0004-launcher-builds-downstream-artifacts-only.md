# Windows 启动器只构建下游交付物

项目把 Windows 单文件启动器作为本机后台与用户侧后续打包的统一控制入口，但“后续打包都使用这个 EXE”只指由当前版本源码形成的下游交付物。启动器可以使用自身内置且经哈希验证的构建上下文生成并导出 Docker 产物及其校验信息；启动器 EXE 自身和其内置资源仍由仓库构建脚本或 CI 生成，启动器不自我构建、自我替换或承担更新发布，以避免自修改程序、递归构建和构建信任来源不清。
