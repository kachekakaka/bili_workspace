# Windows 运行数据必须位于仓库外

新的 Windows 启动器不再把仓库根目录作为默认运行数据根。首次启动必须由用户选择一个不位于当前源码仓库内部的数据根，取消选择则不启动；启动器在该根下分别管理 `config/`、`userdata/` 和 `downloads/`，并持久记住已确认路径，但不静默迁移、删除或改写其他位置的既有数据。这样使配置、凭据、数据库、任务状态与媒体不再受源码更新、构建清理或仓库操作影响，也避免直接运行仓库构建产物时重新把真实数据写回仓库。
