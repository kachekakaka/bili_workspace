# 数据根拥有网络与安全设置

EXE 同级的 `launcher.json` 只是可重建的启动器控制索引，只保存所选数据根、最近导出目录等便利信息；局域网模式、监听地址、端口、可信 Host／代理、Public URL、安全 Cookie 与 HSTS 等运行和安全设置由所选仓库外数据根的 `config/` 持久拥有。删除或丢失 `launcher.json` 后，用户重新选择原数据根即可恢复原有网络、安全和业务状态；启动器必须读取并校验已有配置，非法配置失败关闭，不得用默认值静默覆盖。这样让实际服务状态随数据备份和恢复，而不是依附于某一份 EXE 控制文件。
