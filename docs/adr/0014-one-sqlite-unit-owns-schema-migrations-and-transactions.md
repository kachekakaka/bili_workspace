# SQLite 模式、迁移与事务由单一数据库单元拥有

项目使用一个具体的 SQLite 数据库单元唯一拥有 schema、迁移和事务，认证、任务、作品库、标签与删除记录等具体领域 Store 共享该事务并通过组合协作，不再用独立连接和 `NasStore` 多层继承扩展持久化行为。这个边界使跨领域数据库变更可以原子提交并让迁移只有一条路径，同时刻意不引入 ORM、通用 Repository 接口或为未来数据源预建抽象。
