# 全仓统一 Python 3.11 基线

Windows 启动器、本地开发与测试、CI 和 Docker 全部统一使用 Python 3.11，不再保留 Python 3.12/3.13 兼容矩阵或单独的 3.13 运行边界；仓库内日常开发、测试和 EXE 构建共用唯一 `.venv`，Docker 与 CI 因操作系统隔离而重建环境，但使用同一 Python 3.11 基线和应用依赖锁。项目选择与相邻 `hg_workspace` 对齐并减少重复环境维护，旧 Python 3.13 便携运行链按既定退役流程直接退出，不为过渡另行重建。
