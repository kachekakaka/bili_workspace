# 已知问题与待做需求

本文件只记录未完成事项。每个待办使用“待办 ID：标题”作为二级标题，并且只使用“待确认、待实施、实施中、暂缓”四种状态。

## ARCH-20260830：代码架构整理与重复实现收口

- 状态：待确认
- 当前静态盘点发现后端 81 个路由定义中有 20 个签名重复、21 份冗余定义，部分实际行为依赖 router 注册顺序；`POST /api/tasks/{task_id}/retry` 同时存在三份定义。
- `app/api.py`、`task_ownership_api.py`、`enhancement_api.py`、`refinement_api.py` 与 `catalog_overrides.py` 形成按历史功能叠加的覆盖层；任务扩展还会替换队列私有方法并直接操作私有状态。
- 持久化职责沿 `NasStore`、认证序列化、任务所有权和迁移修复多层继承扩展，核心 Store 与 API 文件体量较大，职责边界和唯一实现路径需要单独审计。
- 后续目标是确认公开行为与数据兼容边界，形成独立的代码架构整理方案，使每项当前能力只有一条运行路径和明确职责所有者，并在每个切片完成时删除被替代实现。
- 本待办独立于当前项目约束、长期文档与测试体系治理；当前只记录问题，不授权创建方案或修改生产代码。
