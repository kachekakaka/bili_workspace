# 项目协作约束

## 语言

- 与用户交流和项目文档使用简体中文。
- 代码注释沿用所在文件的既有语言；不为统一语言批量翻译已有内容。
- 提交标题沿用当前英文惯例。
- 标识符、公开 API 和协议字段按语言生态使用英文。

## 授权与安全

- 未得到明确修改授权时，只进行只读调查。
- 本地只读操作默认允许，包括文件读取、搜索，以及不改变工作区、索引、引用或远端状态的 Git 查询；命令按实际副作用分类，不因使用命令行而自动成为受限操作。
- 访问公开信息的只读联网默认允许，但必须无需认证、不上传本地文件、源码、提示上下文、凭据、真实数据或其他非公开内容，不产生费用，且不发布、提交、留言或改变外部业务与账户状态。
- 用户明确授权“按已确认方案实施”时，该次授权覆盖方案确定范围内的文件新增、修改、重命名和删除、方案列明的普通验证及结果记录、方案列明且来源与副作用不变的普通下载和公开资源获取，以及完成条件满足后的待办状态更新、方案归档、索引更新和精确任务清理。
- 方案保存不构成实施授权。验证升级、新增副作用、候选实质漂移或需要新的语义决定时停止，只申请授权差额；方案关闭不以 commit、push 或 PR 为前提，也不推定这些操作。
- commit（含 amend）、push、PR、发布、安装，以及会覆盖工作区、丢失修改、改写历史或改变远端状态的 Git 操作必须明确授权。commit 授权包含确定变更的必要暂存；未请求 Git 交付时不主动改变索引、分支或历史。
- 需要认证或访问私有资源、上传本地或非公开内容、改变外部状态、产生费用、明显大规模或长时间下载，以及产品进程、真实数据、全量测试和正式认证必须明确授权。
- 已确认方案列明的仓库内精确删除，以及本任务创建且所有权可验证的精确临时产物清理，随实施授权覆盖；方案未列明、超出确定范围、使用宽泛匹配或未解析路径、仓库外非任务产物、所有权不明或涉及真实数据的删除必须取得精确授权。
- 项目停止未来正式发布，不创建新的版本 tag、GitHub Release 或 GHCR 正式镜像；恢复发布能力必须先形成新的显式决策并另行取得修改与发布授权。
- 不覆盖已有工作区修改；发现目标冲突时停止并说明。
- 不把 Cookie、Token、Key、密码、真实用户数据或原始运行现场写入仓库。
- 本地文件调查优先使用 FastCtx 的读取、搜索和查找能力；工具选择本身不构成修改授权。

## 长期入口

- [构建与交付](README.md#构建与交付)
- [项目文档](docs/README.md)
- [测试治理](SoftwareTesting/README.md)
